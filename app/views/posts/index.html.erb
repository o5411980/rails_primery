<h3>views/posts/index.html.erb</h3>
<table>
  <tr>
    <th>Latest ; </th>
  </tr>
</table>
<% @posts.each do |post| %>
  <div class="one_post">
    <tr>
      <td>●</td>
      <td><%= link_to '編集', edit_post_path(post.id) %></td>
      <td><%= post.content %></td>
    </tr>
  </div>
<% end %>
<%= link_to '新規ツィート', new_post_path %>
